name: Deployment Verification Check

Trigger the workflow automatically on every push
on:
push:
branches:
- main # or master, depending on your primary branch name

jobs:
verify_email_step:
runs-on: ubuntu-latest
steps:
# Step 1: Checkout the repository code
- name: Checkout repository
uses: actions/checkout@v4

  # Step 2: The required step with your email address in the name
  - name: 22f3000800@ds.study.iitm.ac.in
    run: |
      echo "Successfully validated the required email step name."
      echo "This action will show up in the 'Actions' tab of your repository."
      
  # Step 3: Confirmation step
  - name: Action complete
    run: echo "Workflow finished."
